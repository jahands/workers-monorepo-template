# Workers Monorepo

For tasks to work with Mise, add this to ~/.zprofile:

```shell
eval "$($HOME/.local/bin/mise activate zsh --shims)"
```

## Notes

- currently stuck on pnpm 10.4.1 due to https://github.com/pnpm/pnpm/issues/9283
