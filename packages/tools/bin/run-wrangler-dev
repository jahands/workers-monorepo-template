#!/bin/sh
set -eu

NAME=$(jq -r '.name' package.json)

# Builds workspace dependencies and copies .dev.vars.example
# to .dev.vars (if missing) before running wrangler dev
exec runx dev wrangler -- --var NAME:"$NAME" "$@"
