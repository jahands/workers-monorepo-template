#!/bin/sh
set -eu

if test -f ./vitest.config.ts; then
	vitest run --testTimeout=15000 --maxConcurrency=20 "$@"
fi
if test -f ./vitest.config.node.ts; then
	vitest run --testTimeout=15000 --maxConcurrency=20 -c ./vitest.config.node.ts "$@"
fi
